name: manatea_flutter_components
description: A new Flutter package project.
version: 1.1.0
homepage:

environment:
  sdk: ">=2.14.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  cached_network_image: ^3.1.0
  flutter:
    sdk: flutter
  nested: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono: ^1.14.0+1

flutter:
